---
title: "Social Rendering Pre-Graph Migration Investigation"
summary: "Queued evidence-first reconstruction of proven pre-graph social rendering and publication behaviour before any repair."
---

# Social Rendering Pre-Graph Migration Investigation

Status: queued only.

Do not fix, migrate, refactor, change production behaviour, restart services,
change schedules, publish, upload, or alter provider payloads from this item.
Begin the investigation only after the active Graph/Meta repair work reaches a
safe completion point.

## Objective

Reconstruct the canonical pre-graph social rendering and publication pipeline
from repository history, state/evidence artifacts, tests, receipts,
documentation, work ledgers, and available historical records. Treat proven
pre-graph behaviour as the reference contract when evaluating current graph
rendering, copy, and publication regressions.

The investigation must determine whether current image/Reel/text/copy defects
come from renderer bugs or from graph migration failing to inherit part of the
canonical pipeline.

## Scope

Pay particular attention to:

- HyperFrames renderer version and skills
- local Reel/image assets
- typography and text fitting
- image composition and layout
- copy generation and formatting
- prepared-image guards
- visual validation
- deterministic template and variant selection
- motion and local audio
- frozen or rendered artifacts
- publication payload construction
- any other capability proven by historical evidence

## Evidence Standard

Do not infer functionality from filenames, intentions, or architectural prose.
Each capability must be backed by one or more of:

- committed source
- tests that exercised the behaviour
- state or database records
- rendered artifacts and hashes
- publication receipts
- validation logs
- operation ledger entries
- memory records with traceable artifact paths

Seed references already identified, not yet reconstructed:

- `memory/2026-07-28.md` records local HyperFrames/FFmpeg Reel rendering and
  audio-identity finishing checkpoints.
- `memory/2026-07-29.md` records Threads image and Instagram Reel copy
  hardening: explicit complete headline/eyebrow or scene copy, measured DOM
  fit/overflow/contrast validation, per-scene reading-time validation, 19/19
  focused Threads checks, 54 Reel tests, and three production zero-write Reel
  diagnostics.
- `memory/2026-08-01.md` records graph binding and shadow evidence where the
  graph consumed the deterministic social publishing engine and preserved
  zero-write equivalence for representative samples.
- `memory/2026-08-05.md` records later Instagram Reel graph ownership work and
  a live edge where dynamic allocation/rendering remained healthy while other
  publication blockers existed.

## Required Ledger

Produce a migration ledger with this exact shape:

| Canonical capability | Original implementation | Evidence/tests | Current graph equivalent | Migrated / missing / changed / regressed | Consequence | Recommended repair |
|---|---|---|---|---|---|---|

The final report must separate:

- proven migrated behaviour
- missing behaviour
- changed but acceptable behaviour
- changed and risky behaviour
- confirmed regressions
- open questions requiring more evidence

## Boundaries

This item is an investigation/backlog item only. Any repair, migration,
refactor, production behaviour change, service lifecycle action, commit, push,
deployment, provider write, upload, or schedule mutation requires a separate
explicit approval after the evidence report is delivered.
